/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // i18n kan legges til senere når flerspråklig støtte implementeres
  // i18n: {
  //   locales: ['nb', 'nn', 'en'],
  //   defaultLocale: 'nb',
  // },
}

module.exports = nextConfig
